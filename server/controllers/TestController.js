import BuildingModel from "../models/building";

export class TestController {
    // static async findDataByLocations(req, res, next) {
    //     try {
    //         const { latitude, longitude } = req.query;
    //         const data = await BuildingModel.findDataByLocations(latitude, longitude);
    //         res.status(200).json(data);
    //     } catch (err) {
    //         next(err);
    //     }

    // }
}